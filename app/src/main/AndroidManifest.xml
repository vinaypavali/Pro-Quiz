<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quiz.proquiz">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_new_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_new_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProQuiz">

        <activity android:name=".Auth.LoginActivity">
        </activity>

        <activity android:name=".Auth.SignupActivity">
        </activity>


        <activity android:name=".QuizActivity"
            android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".ForgotPassword">

        </activity>

        <activity android:name=".ResultActivity"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".ProfileFragment">

        </activity>

        <activity android:name=".RankFragment">

        </activity>

        <activity android:name=".WalletFragment">

        </activity>
        <activity android:name=".SplashScreen"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>